# Screenshots

These images are referenced by [`SYSTEM_WORK.md`](../../SYSTEM_WORK.md). They are
captured manually (no headless browser runs in CI for this repo). To (re)capture:

1. Start the stack — see [`RUN_THE_PROJECT.md`](../../RUN_THE_PROJECT.md).
2. Log in at <http://localhost:3000/login> as `oxford@gmail.com` / `@oxford123#`.
3. Capture each screen at ~1440px width and save with the exact filename below.

| File | Screen to capture |
|---|---|
| `dashboard.png` | `/oxford/home` (tenant dashboard) |
| `grade-prediction.png` | `/oxford/modules/grade-prediction` after running a prediction |
| `subject-prediction.png` | `/oxford/subject-prediction` after a recommendation |
| `career-prediction.png` | `/oxford/modules/career-guidance` after a prediction |
| `growth-potential.png` | `/oxford/modules/growth-potential` after a 9-Box evaluation |
| `batch-prediction.png` | `/oxford/modules/batch-prediction` (prediction + KPIs) |
| `admin-modules.png` | `/dashboard/admin/modules` (super-admin module grid) |
| `role-based-login.png` | `/dashboard/admin/roles` (role impersonation grid) |

> Windows quick capture: `Win`+`Shift`+`S`, drag the region, paste into an editor, save as PNG here.
